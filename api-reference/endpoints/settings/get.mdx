---
title: 'Get Account Settings'
api: 'GET /api/v1/settings'
description: 'Retrieve comprehensive account settings and configuration'
---

## Overview

Returns all configuration and settings for the authenticated firm including company details, invoice series, e-Factura status, bank accounts, available currencies, and subscription information.

This is a read-only endpoint that provides a complete view of your account configuration.

## Response

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="Properties">
    <ResponseField name="object" type="string">
      Always `settings`
    </ResponseField>
    
    <ResponseField name="firm" type="object">
      Company details and contact information
      
      <Expandable title="Firm Properties">
        <ResponseField name="name" type="string">
          Display name (trading name if exists, otherwise legal name)
        </ResponseField>
        
        <ResponseField name="legal_name" type="string">
          Official registered company name (from company registration)
        </ResponseField>
        
        <ResponseField name="trade_name" type="string">
          Work point/branch name (optional, can be null)
        </ResponseField>
        
        <ResponseField name="cui" type="string">
          Tax identification number (CUI/CIF)
        </ResponseField>
        
        <ResponseField name="rc" type="string">
          Company registration number
        </ResponseField>
        
        <ResponseField name="address" type="string">
          Company address
        </ResponseField>
        
        <ResponseField name="city" type="string">
          City
        </ResponseField>
        
        <ResponseField name="county" type="string">
          County or state
        </ResponseField>
        
        <ResponseField name="country" type="string">
          Country code (ISO 3166-1 alpha-2)
        </ResponseField>
        
        <ResponseField name="postal_code" type="string">
          Postal or ZIP code
        </ResponseField>
        
        <ResponseField name="phone" type="string">
          Contact phone number
        </ResponseField>
        
        <ResponseField name="email" type="string">
          Contact email address
        </ResponseField>
        
        <ResponseField name="website" type="string">
          Company website URL
        </ResponseField>
        
        <ResponseField name="contact_person" type="string">
          Primary contact person name
        </ResponseField>
        
        <ResponseField name="contact_email" type="string">
          Primary contact email
        </ResponseField>
      </Expandable>
    </ResponseField>
    
    <ResponseField name="invoice_series" type="array">
      List of configured invoice series
      
      <Expandable title="Series Object">
        <ResponseField name="id" type="string">
          Series CzUid (use this for series_id parameter)
        </ResponseField>
        
        <ResponseField name="name" type="string">
          Series name/prefix
        </ResponseField>
        
        <ResponseField name="document_type" type="string">
          Document type: `fiscal` or `proforma`
        </ResponseField>
        
        <ResponseField name="format" type="string">
          Number format pattern
        </ResponseField>
        
        <ResponseField name="next_number" type="integer">
          Next available number in series
        </ResponseField>
        
        <ResponseField name="is_primary" type="boolean">
          Whether this is the primary series for its document type
        </ResponseField>
        
        <ResponseField name="is_active" type="boolean">
          Whether the series is active
        </ResponseField>
      </Expandable>
    </ResponseField>
    
    <ResponseField name="efactura" type="object">
      E-Factura configuration and OAuth status
      
      <Expandable title="E-Factura Properties">
        <ResponseField name="enabled" type="boolean">
          Whether e-Factura is enabled for the account
        </ResponseField>
        
        <ResponseField name="oauth_configured" type="boolean">
          Whether OAuth is properly configured
        </ResponseField>
        
        <ResponseField name="test_mode_available" type="boolean">
          Whether test environment credentials are configured
        </ResponseField>
        
        <ResponseField name="live_mode_available" type="boolean">
          Whether production environment credentials are configured
        </ResponseField>
        
        <ResponseField name="oauth_created_at" type="string">
          OAuth token creation date
        </ResponseField>
        
        <ResponseField name="oauth_expires_at" type="string">
          OAuth access token expiration date
        </ResponseField>
        
        <ResponseField name="settings" type="object">
          E-Factura specific settings
          
          <Expandable title="Settings Properties">
            <ResponseField name="environment" type="string">
              Default environment: `test` or `live`
            </ResponseField>
            
            <ResponseField name="auto_send" type="boolean">
              Whether invoices are automatically sent to ANAF
            </ResponseField>
            
            <ResponseField name="auto_send_timing" type="string">
              Timing for automatic sending: `immediate` or `delayed`
            </ResponseField>
            
            <ResponseField name="send_to_public_institutions" type="boolean">
              Whether to send invoices to public institutions automatically
            </ResponseField>
          </Expandable>
        </ResponseField>
      </Expandable>
    </ResponseField>
    
    <ResponseField name="bank_accounts" type="array">
      List of configured bank accounts
      
      <Expandable title="Bank Account Object">
        <ResponseField name="id" type="string">
          Account CzUid
        </ResponseField>
        
        <ResponseField name="bank_name" type="string">
          Bank name
        </ResponseField>
        
        <ResponseField name="iban" type="string">
          IBAN account number
        </ResponseField>
        
        <ResponseField name="currency" type="string">
          Account currency code
        </ResponseField>
        
        <ResponseField name="is_primary" type="boolean">
          Whether this is the primary account
        </ResponseField>
        
        <ResponseField name="is_active" type="boolean">
          Whether the account is active
        </ResponseField>
      </Expandable>
    </ResponseField>
    
    <ResponseField name="subscription" type="object">
      Current subscription information
      
      <Expandable title="Subscription Properties">
        <ResponseField name="plan" type="string">
          Current plan name
        </ResponseField>
        
        <ResponseField name="status" type="string">
          Subscription status: `active` or `inactive`
        </ResponseField>
        
        <ResponseField name="expires_at" type="string">
          Subscription expiration date
        </ResponseField>
        
        <ResponseField name="invoices_limit" type="integer">
          Monthly invoice limit (null if unlimited)
        </ResponseField>
        
        <ResponseField name="invoices_used" type="integer">
          Invoices used this period
        </ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Response metadata including version and response time
</ResponseField>

<RequestExample>

```bash cURL
curl https://api.contazen.ro/v1/settings \
  -H "Authorization: Bearer sk_live_YOUR_API_KEY"
```

```javascript Node.js
const response = await fetch('https://api.contazen.ro/v1/settings', {
  headers: {
    'Authorization': 'Bearer sk_live_YOUR_API_KEY'
  }
});

const settings = await response.json();
```

```python Python
import requests

response = requests.get(
    'https://api.contazen.ro/v1/settings',
    headers={'Authorization': 'Bearer sk_live_YOUR_API_KEY'}
)

settings = response.json()
```

```php PHP
$ch = curl_init('https://api.contazen.ro/v1/settings');
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Authorization: Bearer sk_live_YOUR_API_KEY'
]);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
$settings = json_decode($response, true);
```

</RequestExample>

<ResponseExample>

```json Success Response
{
  "success": true,
  "data": {
    "object": "settings",
    "firm": {
      "name": "Exemplu Company",
      "legal_name": "Exemplu Company SRL",
      "trade_name": "Exemplu Company",
      "cui": "RO12345678",
      "rc": "J40/1234/2020",
      "address": "Str. Exemplu nr. 123",
      "city": "București, Sector 1",
      "county": "București",
      "country": "RO",
      "postal_code": "010101",
      "phone": "+40721234567",
      "email": "contact@exemplu.ro",
      "website": "https://www.exemplu.ro",
      "contact_person": "Ion Popescu",
      "contact_email": "ion.popescu@exemplu.ro"
    },
    "invoice_series": [
      {
        "id": "yMzhgkh",
        "name": "CTZ",
        "document_type": "fiscal",
        "format": "CTZ-{year}-{number}",
        "next_number": 145,
        "is_primary": true,
        "is_active": true
      },
      {
        "id": "xKwpf9j",
        "name": "PRO",
        "document_type": "proforma",
        "format": "PRO-{year}-{number}",
        "next_number": 23,
        "is_primary": true,
        "is_active": true
      },
      {
        "id": "zNmq8kl",
        "name": "TEST",
        "document_type": "fiscal",
        "format": "TEST-{year}-{number}",
        "next_number": 1,
        "is_primary": false,
        "is_active": true
      }
    ],
    "efactura": {
      "enabled": true,
      "oauth_configured": true,
      "test_mode_available": true,
      "live_mode_available": true,
      "oauth_created_at": "2024-01-15 10:30:00",
      "oauth_expires_at": "2024-01-15 11:30:00",
      "settings": {
        "environment": "live",
        "auto_send": true,
        "auto_send_timing": "immediate",
        "send_to_public_institutions": false
      }
    },
    "bank_accounts": [
      {
        "id": "bnk_1a2b3c",
        "bank_name": "Banca Transilvania",
        "iban": "RO49BTRL1234567890123456",
        "currency": "RON",
        "is_primary": true,
        "is_active": true
      },
      {
        "id": "bnk_4d5e6f",
        "bank_name": "Revolut",
        "iban": "RO98REVO9876543210987654",
        "currency": "EUR",
        "is_primary": false,
        "is_active": true
      }
    ],
    "subscription": {
      "plan": "Professional",
      "status": "active",
      "expires_at": "2024-12-31",
      "invoices_limit": 500,
      "invoices_used": 127
    }
  },
  "meta": {
    "version": "v1",
    "response_time": "45.67ms"
  }
}
```

</ResponseExample>

## Use Cases

### Setting Up Your Integration

Use this endpoint to:
- Retrieve available invoice series for creating invoices
- Check e-Factura OAuth configuration status
- Get list of bank accounts for payment instructions
- Verify subscription limits before creating invoices
- Discover available currencies for multi-currency invoices

### Example: Check E-Factura Status

```javascript
const response = await fetch('https://api.contazen.ro/v1/settings', {
  headers: { 'Authorization': 'Bearer sk_live_YOUR_API_KEY' }
});

const data = await response.json();

if (data.data.efactura.oauth_configured) {
  console.log('E-Factura is configured and ready');
  console.log('Available environments:', {
    test: data.data.efactura.test_mode_available,
    live: data.data.efactura.live_mode_available
  });
} else {
  console.log('E-Factura OAuth needs configuration');
}
```

### Example: Get Primary Invoice Series

```javascript
const settings = await getSettings(); // Your API call

const primaryFiscalSeries = settings.data.invoice_series.find(
  series => series.document_type === 'fiscal' && series.is_primary
);

console.log('Use this series_id for fiscal invoices:', primaryFiscalSeries.id);
```

<Note>
  This endpoint is read-only. To modify settings, use the Contazen web interface at https://app.contazen.ro
</Note>