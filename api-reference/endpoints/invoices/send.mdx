---
title: 'Send Invoice'
api: 'POST /api/v1/invoices/{id}/send'
description: 'Send an invoice by email'
---

## Request

<ParamField path="id" type="string" required>
  The invoice ID (CzUid) to send
</ParamField>

<ParamField body="email" type="string">
  Override recipient email address. If not provided, the client's email address will be used.
</ParamField>

<Note>
  The invoice must meet the following requirements to be sent:
  - Not a draft (`is_draft = false`)
  - Not voided/cancelled (`is_storno = false`)
  - Not deleted (`is_deleted = false`)
  - Client must have a valid email address (unless overridden)
</Note>

## Response

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="Properties">
    <ResponseField name="sent" type="boolean">
      Whether the email was sent successfully
    </ResponseField>
    
    <ResponseField name="email" type="string">
      The email address the invoice was sent to
    </ResponseField>
    
    <ResponseField name="message" type="string">
      Success message
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Response metadata including version and response time
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/send \
  -H "Authorization: Bearer sk_live_YOUR_API_KEY" \
  -H "Content-Type: application/json"
```

```bash cURL with Override Email
curl -X POST https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/send \
  -H "Authorization: Bearer sk_live_YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "accounting@techsolutions.ro"
  }'
```

```javascript Node.js
const response = await fetch('https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/send', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer sk_live_YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    email: 'accounting@techsolutions.ro' // optional
  })
});

const data = await response.json();
```

```python Python
import requests

response = requests.post(
    'https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/send',
    json={
        'email': 'accounting@techsolutions.ro'  # optional
    },
    headers={'Authorization': 'Bearer sk_live_YOUR_API_KEY'}
)

data = response.json()
```

```php PHP
$ch = curl_init('https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/send');
curl_setopt($ch, CURLOPT_POST, true);
curl_setopt($ch, CURLOPT_POSTFIELDS, json_encode([
    'email' => 'accounting@techsolutions.ro'  // optional
]));
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Authorization: Bearer sk_live_YOUR_API_KEY',
    'Content-Type: application/json'
]);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
$data = json_decode($response, true);
```

</RequestExample>

<ResponseExample>

```json Success Response
{
  "success": true,
  "data": {
    "sent": true,
    "email": "contact@techsolutions.ro",
    "message": "Invoice sent successfully"
  },
  "meta": {
    "version": "v1",
    "response_time": "456.78ms"
  }
}
```

```json Error Response - Draft Invoice
{
  "success": false,
  "error": {
    "message": "Cannot send draft invoice",
    "type": "validation_error",
    "code": "invalid_invoice_status",
    "status": 400
  },
  "meta": {
    "version": "v1",
    "response_time": "12.34ms"
  }
}
```

```json Error Response - No Email
{
  "success": false,
  "error": {
    "message": "Email address is required",
    "type": "validation_error",
    "code": "validation_error",
    "status": 400,
    "errors": {
      "email": "Email address is required"
    }
  },
  "meta": {
    "version": "v1",
    "response_time": "10.23ms"
  }
}
```

```json Error Response - Email Failed
{
  "success": false,
  "error": {
    "message": "Failed to send email",
    "type": "api_error",
    "code": "email_send_failed",
    "status": 500
  },
  "meta": {
    "version": "v1",
    "response_time": "234.56ms"
  }
}
```

</ResponseExample>

## Automatic Sending

You can also automatically send an invoice when creating it by including the `send_email` parameter:

```bash
curl -X POST https://api.contazen.ro/v1/invoices \
  -H "Authorization: Bearer sk_live_YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "client_id": "1a2b3c4d5e",
    "items": [...],
    "send_email": true,
    "email_address": "optional@override.com"
  }'
```

The response will include an `email_sent` field indicating whether the email was sent successfully.