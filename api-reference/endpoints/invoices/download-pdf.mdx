---
title: 'Download Invoice PDF'
api: 'GET /invoices/{id}/pdf'
description: 'Download an invoice as PDF'
---

## Request

<ParamField path="id" type="string" required>
  The invoice ID (ID) to download
</ParamField>

<ParamField query="mode" type="string" default="download">
  Download mode:
  - `download` - Returns the PDF file directly (default)
  - `url` - Returns a temporary signed URL to download the PDF
</ParamField>

<ParamField query="options" type="string">
  PDF generation options (optional). Can include customization parameters.
</ParamField>

<Warning>
  Draft invoices cannot be downloaded as PDF. The invoice must be finalized first.
</Warning>

## Response

### Download Mode (default)

When `mode=download` or not specified, the response is the PDF file itself:

- **Content-Type**: `application/pdf`
- **Content-Disposition**: `attachment; filename="invoice_number.pdf"`
- **X-Invoice-ID**: The invoice ID in the response header

### URL Mode

When `mode=url`, the response is a JSON object containing a temporary download URL:

<ResponseField name="success" type="boolean">
  Indicates if the request was successful
</ResponseField>

<ResponseField name="data" type="object">
  <Expandable title="Properties">
    <ResponseField name="id" type="string">
      The invoice ID
    </ResponseField>
    
    <ResponseField name="object" type="string">
      Always `invoice_pdf`
    </ResponseField>
    
    <ResponseField name="url" type="string">
      Temporary signed URL to download the PDF
    </ResponseField>
    
    <ResponseField name="expires_at" type="integer">
      Unix timestamp when the URL expires (1 hour from generation)
    </ResponseField>
    
    <ResponseField name="content_type" type="string">
      Always `application/pdf`
    </ResponseField>
    
    <ResponseField name="filename" type="string">
      The suggested filename for the PDF
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="meta" type="object">
  Response metadata including version and response time
</ResponseField>

<RequestExample>

```bash cURL - Direct Download
curl https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/pdf \
  -H "Authorization: Bearer sk_live_YOUR_API_KEY" \
  -o invoice.pdf
```

```bash cURL - Get URL
curl https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/pdf?mode=url \
  -H "Authorization: Bearer sk_live_YOUR_API_KEY"
```

```javascript Node.js - Direct Download
const response = await fetch('https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/pdf', {
  headers: {
    'Authorization': 'Bearer sk_live_YOUR_API_KEY'
  }
});

if (response.ok) {
  const blob = await response.blob();
  // Save or process the PDF blob
  const url = window.URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'invoice.pdf';
  a.click();
}
```

```javascript Node.js - Get URL
const response = await fetch('https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/pdf?mode=url', {
  headers: {
    'Authorization': 'Bearer sk_live_YOUR_API_KEY'
  }
});

const data = await response.json();
console.log('PDF URL:', data.data.url);
```

```python Python - Direct Download
import requests

response = requests.get(
    'https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/pdf',
    headers={'Authorization': 'Bearer sk_live_YOUR_API_KEY'}
)

if response.status_code == 200:
    with open('invoice.pdf', 'wb') as f:
        f.write(response.content)
```

```python Python - Get URL
import requests

response = requests.get(
    'https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/pdf',
    params={'mode': 'url'},
    headers={'Authorization': 'Bearer sk_live_YOUR_API_KEY'}
)

data = response.json()
pdf_url = data['data']['url']
```

```php PHP - Direct Download
$ch = curl_init('https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/pdf');
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Authorization: Bearer sk_live_YOUR_API_KEY'
]);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$pdfContent = curl_exec($ch);
file_put_contents('invoice.pdf', $pdfContent);
```

```php PHP - Get URL
$ch = curl_init('https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/pdf?mode=url');
curl_setopt($ch, CURLOPT_HTTPHEADER, [
    'Authorization: Bearer sk_live_YOUR_API_KEY'
]);
curl_setopt($ch, CURLOPT_RETURNTRANSFER, true);

$response = curl_exec($ch);
$data = json_decode($response, true);
$pdfUrl = $data['data']['url'];
```

</RequestExample>

<ResponseExample>

```json URL Mode Response
{
  "success": true,
  "data": {
    "id": "inv_9z8y7x6w5v",
    "object": "invoice_pdf",
    "url": "https://api.contazen.ro/v1/invoices/inv_9z8y7x6w5v/pdf/download?token=abc123def456&expires=1710464400&signature=xyz789",
    "expires_at": 1710464400,
    "content_type": "application/pdf",
    "filename": "CTZ-2024-00123.pdf"
  },
  "meta": {
    "version": "v1",
    "response_time": "23.45ms"
  }
}
```

```json Error Response - Draft Invoice
{
  "success": false,
  "error": {
    "message": "Cannot export draft invoice",
    "type": "invalid_request_error",
    "code": "invoice_is_draft",
    "status": 400
  },
  "meta": {
    "version": "v1",
    "response_time": "12.34ms"
  }
}
```

```json Error Response - Not Found
{
  "success": false,
  "error": {
    "message": "Invoice not found",
    "type": "invalid_request_error",
    "code": "resource_missing",
    "param": "id",
    "status": 404
  },
  "meta": {
    "version": "v1",
    "response_time": "10.12ms"
  }
}
```

</ResponseExample>

## Use Cases

### Direct Download
Best for:
- Desktop applications
- Immediate file saving
- One-time downloads

### URL Mode
Best for:
- Webhook integrations
- Async processing
- Sharing download links
- Mobile applications

<Note>
  The temporary URL expires after 1 hour for security. Generate a new URL if needed after expiration.
</Note>